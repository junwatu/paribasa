#!/usr/bin/env node
'use strict'

const paribasan = require('../index.js')

if (process.argv[2] === 'search') {
  paribasan.search(process.argv[3])
} else {
  let randParibasan = paribasan.get()
  console.log(`"${randParibasan.paribasa}" - ${randParibasan.jawa}`)
}
